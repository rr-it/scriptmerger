plugin.tx_scriptmerger {
	css {
		enable = 1

		# minification
		minify {
			enable = 1
			ignore =
		}

		# gzip compression
		compress {
			enable = 1
			ignore =
		}

		# merging
		merge {
			enable = 1
			ignore =
		}
	}

	javascript {
		enable = 1
		parseBody = 0
		addBeforeBody = 0

		# minification
		minify {
			enable = 1
			ignore = \?
		}

		# gzip compression
		compress {
			enable = 1
			ignore = \?
		}

		# merging
		merge {
			enable = 1
			ignore = \?
		}
	}
}
